{
  "name": "insider-threat-blockchain",
  "version": "1.0.0",
  "private": true,
  "description": "Local Hardhat project with the AuditLog contract for the insider-threat POC",
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "hardhat": "2.22.17",
    "@nomicfoundation/hardhat-ethers": "3.0.8",
    "ethers": "6.13.4"
  }
}
