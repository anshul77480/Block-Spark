# Pinned, mutually compatible versions (Python 3.11 / 3.12).
# SHAP + numpy + scikit-learn are version-sensitive: this set installs and
# imports together. Do not bump one without checking the others.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20

# ML / data
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2
pandas==2.2.3
shap==0.46.0
joblib==1.4.2
pyod==2.0.3            # optional fallback anomaly detector

# storage
SQLAlchemy==2.0.36
psycopg2-binary==2.9.9

# auth
PyJWT==2.10.1
passlib==1.7.4
bcrypt==4.0.1         # 4.0.x is the version passlib 1.7.4 reads cleanly

# blockchain client
web3==7.6.0

# misc
httpx==0.28.1
python-dotenv==1.0.1
